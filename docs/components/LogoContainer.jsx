import React from 'react';
import Logo from '../svgs/Logo';

const LogoContainer = () => {
  return (
    <div style={{ width: '180px', paddingBottom: '1rem' }}>
      <Logo />
    </div>
  );
};

export default LogoContainer;
